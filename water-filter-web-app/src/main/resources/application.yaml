logging:
  level:
    org.springframework.security: DEBUG
    
spring:
  application:
    name: water-filter-web
  datasource:
    url: jdbc:h2:mem:testdb
  h2:
    console:
      enabled: true
      path: /h2-console
  jpa:
    hibernate:
      ddl-auto: update

server:
  port: 8000
